node_modules/
web/dist/

# secrets / local config
.env
.env.local
bot.config.json
accounts.json

# per-user generated data (keep .skill/ — it's the shareable skill pack)
projects/
requirements/
# tài liệu .docx docgen xuất ra khi phát triển — hàng MB mỗi tệp, không commit
exports/
docgen-work/

# Yarn 4 (node-modules linker): commit releases + config + lockfile, ignore the rest
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*

# os
.DS_Store
